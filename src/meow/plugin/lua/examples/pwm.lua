pwm = require("pwm")

-- Налаштувати pwm (канал, частота, розрядність)
pwm.setup(0, 3000, 8) 

-- Приєднати 12-й пін до 0-вого каналу pwm
pwm.attach(12, 0) 

-- Встановити 20% заповнення для 0-вого каналу
pwm.set(0, 20) 

-- Налаштувати пін на вихід
pin.output(13) 

function update()
	-- Змінити стан піна на протилежний
    pin.toggle(13)
	
	-- Сон в мс
    mcu.sleep(1000) 
end